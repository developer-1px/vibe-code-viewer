/**
 * CodeSegment Entity
 *
 * Represents a syntax-highlighted token within a code line
 */

export type { CodeSegment, SegmentKind, SegmentStyle } from './model/types';
export { buildSegmentStyle } from './lib/styleBuilder';
